import { MenuCategoryMappingDto } from './menu.category.mapping.dto';

describe('MenuCategoryMappingDto', () => {
  it('should be defined', () => {
    expect(new MenuCategoryMappingDto()).toBeDefined();
  });
});
