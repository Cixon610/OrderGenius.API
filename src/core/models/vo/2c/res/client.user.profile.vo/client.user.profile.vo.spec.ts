import { ClientUserProfileVo } from './client.user.profile.vo';

describe('ClientUserProfileVo', () => {
  it('should be defined', () => {
    expect(new ClientUserProfileVo()).toBeDefined();
  });
});
