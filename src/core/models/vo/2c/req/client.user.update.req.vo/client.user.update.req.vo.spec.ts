import { ClientUserUpdateReqVo } from './client.user.update.req.vo';

describe('ClientUserUpdateReqVo', () => {
  it('should be defined', () => {
    expect(new ClientUserUpdateReqVo()).toBeDefined();
  });
});
